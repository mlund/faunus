#!/bin/bash
cd `dirname $0`
exe="../../src/examples/titration/pkaGC_sphere"
if [ -x $exe ]; then
  cp test.start confout.aam
  cp test.groups gcgroup.conf
  OMP_NUM_THREADS=1 $exe test.conf
  exit
fi
exit 1

